import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },
  mapContainer: {
    marginBottom: 80, // Adds space above and below the map
  },
});
